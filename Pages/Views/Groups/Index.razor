@page "/Groups/Index"
@using MalaikaSchool.Data.Models

<h2>Index</h2>

<p>
    <NavLink href="/Groups/Create">Create New</NavLink>
</p>
<table class="table">
    <tr>
        <th>
            Intake Name
        </th>
       
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @item.Name
    </td>
   

    <td>
        <NavLink href="/Groups/Edit/{Id:int}" @key="@item.Id">Edit</NavLink>|
        <NavLink href="/Groups/Details" @key="@item.Id">Details</NavLink>|
        <NavLink href="/Groups/Delete" @key="@item.Id">Delete</NavLink>
    </td>
</tr>
    }

</table>
@code {

    private List<Group> Model;


    protected override  Task OnInitializedAsync()
    {
        Model = new List<Group>();


        return base.OnInitializedAsync();
    }

}